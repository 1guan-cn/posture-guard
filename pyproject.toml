[project]
name = "posture-guard"
version = "0.1.0"
description = "本地摄像头颈椎前倾与久坐监测，纯几何判定，无云端依赖。"
readme = "README.md"
license = "MIT"
license-files = ["LICENSE"]
requires-python = ">=3.11"
dependencies = [
    "mediapipe>=0.10.35",
    "numpy>=2.4.4",
    "opencv-python>=4.13.0.92",
]
